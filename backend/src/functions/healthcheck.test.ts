import { describe, expect, it } from "vitest";

describe.todo("Backend Healthcheck", () => {
  it("should run correctly", () => {
    const IamCorrect = true;
    expect(IamCorrect).toBe(true);
  });
});
